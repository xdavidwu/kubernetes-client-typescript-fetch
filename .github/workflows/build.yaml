name: build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    container: alpine:3.20
    steps:
      - uses: actions/checkout@v4
      - name: prepare
        run: apk add npm
      - name: build
        # prepare script builds
        run: npm ci
